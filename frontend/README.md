# Cortex frontend

Next.js + TypeScript + Tailwind chat UI with the live agent trace panel.

Setup, environment, and demo instructions live in the [root README](../README.md). Run it with `make web` from the repository root (or `npm run dev` here), with the Go API from `make dev` on :8080.
